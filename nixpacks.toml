[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "nss", "nspr", "at-spi2-atk", "cups", "libdrm", "dbus", "mesa", "libxkbcommon", "pango", "cairo", "alsa-lib", "gtk3", "liberation_ttf"]

[phases.build]
cmds = ["pip install -r requirements.txt", "playwright install chromium 2>/dev/null || true"]

[start]
cmd = "python app.py"
