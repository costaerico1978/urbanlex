[phases.setup]
nixPkgs = ["chromium", "nss", "nspr", "atk", "cups", "libdrm", "dbus", "mesa", "libxkbcommon", "pango", "cairo", "alsa-lib"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python -m playwright install chromium"]
